export default function LoadingSceneComponent() {
    return (
        <section className="loading-icon fixed w-full h-full top-0 left-0 right-0 bottom-0 flex justify-center items-center backdrop-blur-sm">
        </section>
    );
}
